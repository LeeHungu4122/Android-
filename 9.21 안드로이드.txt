weight => 화면에 차는 비율

set 은 1명, add는 여러명 가능.

익명내부클래스에서 외부 명령을 실행시킬 때 내부에 해당 클래스가 없다면 앞부분생략하고 클래스명만 기입해 실행 가능.

findViewById ==> find로 명명한 것은 xml에서 지정해 놓은 버튼에 해당하는 부분을 찾아 보여주는 형태이므로?

Listener 로 전달하고 싶은 지역변수는 final 로 처리해야 한다.(상수취급되어 지역변수라도 수명을 늘려 사용할 수 있다.)

